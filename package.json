{
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cypress": "^15.2.0",
    "start-server-and-test": "^2.1.2"
  },
  "scripts": {
    "start:dev": "concurrently \"npm:backend\" \"npm:frontend\"",
    "backend": "cd backend && node server.js",
    "frontend": "cd frontend && npm start",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  }
}
